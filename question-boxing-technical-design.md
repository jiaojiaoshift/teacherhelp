# TeachHelper 智能框题技术说明

## 1. 文档信息

- 文档状态：当前实现说明
- 基准日期：2026-08-16
- 适用范围：Web 工作台中的 PDF 自动框题阶段
- 不包含：跨页候选判定细节、OCR 后的题目分类、答案匹配和题库持久化

本文档描述当前代码实际执行的框题流程。运行时模型、路由和推理强度由本机 OpenAI-compatible/ccSwitch 配置决定，不在本文档中保存任何密钥、鉴权信息或中转站地址。

当前在线日志确认的模型为 `gpt-5.6-sol`。模型名称属于运行状态，不是框题数据协议的一部分。

## 2. 目标与原则

框题阶段需要从试卷页面中识别真正需要作答的独立题目，并为每道题生成准确的页面边界框。

核心原则：

1. 先理解整页文字和版面语义，再确定题目边界。
2. 题号、题干、选项、小问、公式和题图应属于同一个题框。
3. 知识点梳理、目录、页眉页脚、答案解析和纯讲解不得作为题目。
4. 相邻题框不得重叠，也不得吞入下一题题号。
5. 页首承接上一页的内容先作为续题片段框出，随后交给跨页流程合并。
6. AI 负责视觉理解，本地规则负责校验、补漏和几何稳定化。

## 3. 总体流程

```text
PDF 文件
  -> 逐页渲染为图片
  -> 提取 PDF 原生文字及坐标（如果存在）
  -> 按页并发调用框题 API
      -> 第一阶段：整页坐标 OCR 与语义角色标注
      -> 本地 OCR 角色纠错
      -> 第二阶段：原图 + OCR 坐标 + 题号锚点联合框题
      -> 语义过滤与漏框补齐
  -> 本地边界框规范化、去重、拆分重叠和留白扩展
  -> 转换为页面像素坐标并生成 QuestionDraft
  -> 后续跨页检测与人工复核
```

主要代码入口：

- `app/page.tsx`：按页并发、进度更新和结果写入工作区
- `app/api/ai/detect-question-boxes/route.ts`：框题 API
- `lib/ai/teachhelper-codex-agent.ts`：两阶段 AI 调用与语义后处理
- `lib/ai/teachhelper-ai-prompts.ts`：OCR、框题和跨页提示词
- `lib/services/analysis-service.ts`：边界框几何后处理与题目草稿生成
- `lib/pdf/pdf-renderer.ts`：PDF 页面渲染和原生文字提取

## 4. 输入数据

每一页框题请求包含：

```ts
{
  pageId: string;
  imageDataUrl: string;
  subjectScope?: string;
  textLines?: PageTextLine[];
}
```

其中：

- `imageDataUrl` 是页面渲染图，供视觉模型读取公式、图表和版面。
- `subjectScope` 是当前选择的学科范围，例如高中物理。
- `textLines` 是 PDF 原生文字层提取出的文字行及坐标；扫描 PDF 可能没有该数据。
- 所有传给模型的坐标均使用 `0-1000` 归一化坐标系，左上角为原点。

## 5. 第一阶段：整页坐标 OCR

### 5.1 是否执行完整 OCR

系统首先规范化 PDF 原生文字行。如果输入文字行已经包含语义 `role`，则直接复用；否则调用视觉模型执行完整页面 OCR。

OCR 结果按页面图片指纹缓存在当前服务进程内。同一张页面图片在缓存有效期间不会重复执行第一阶段。调用失败时会清除对应缓存，允许后续重试。

### 5.2 OCR 输出角色

每一行文字必须携带坐标，并可具有以下角色：

| role | 含义 |
| --- | --- |
| `question_anchor` | 独立题目的顶层题号与首行 |
| `question_content` | 题干、选项、小问、题图说明或命题来源 |
| `question_continuation` | 页首承接上一页、且本页没有新顶层题号的内容 |
| `knowledge_note` | 知识点梳理、定义、公式归纳、讲解或例题解析 |
| `directory` | 目录或索引 |
| `header` / `footer` | 页眉或页脚 |
| `other` | 无法归入以上类别的文字 |

### 5.3 当前 OCR 提示词

以下是当前静态提示词。实际请求还会动态附加经过裁剪的 PDF 原生坐标文字 JSON。

```text
你是试卷页面坐标 OCR 助手。
必须查看页面图像并完整识别整页，按阅读顺序返回所有有意义的文字行，包括题号、题干、选项、公式旁文字、页首续题、页尾未完文字、知识点梳理、目录、页眉和页脚。
不要把整页合并成一段；每一行必须保留独立的 0-1000 归一化坐标，左上角为原点。
每行必须标注 role：question_anchor 表示一道独立题目的顶层题号与首行；question_content 表示题干、选项、小问、题图说明或命题来源；question_continuation 表示页首承接上一页且本页没有新顶层题号的题目内容；knowledge_note 表示知识点梳理、定义总结、公式归纳、讲解或例题解析；directory 表示目录或索引；header/footer 表示页眉页脚；无法归类使用 other。
只有要求作答、求解、判断、选择、填空、证明或计算的独立练习才属于题目；纯知识讲解、知识清单、章节目录、答案解析和示例说明不属于题目。
PDF 原生坐标文字仅用于校对字符与大致位置，不能替代查看图像，也不能直接照抄其错误阅读顺序。
PDF 原生坐标文字：<动态 JSON>
忽略纯装饰线条。
返回严格 JSON：{ "lines": [{ "text": "12. 如图所示", "role": "question_anchor", "normalizedBBox": { "x1": 80, "y1": 120, "x2": 920, "y2": 160 } }] }。
无法辨认的行不要编造；可以返回空数组。
```

### 5.4 OCR 结果纠错

视觉模型可能把练习题误标为知识点或普通文字。本地纠错会结合以下证据恢复题目角色：

- 顶层数字题号格式
- 附近的练习/题目提示
- 题干中的作答、求解、判断等语义
- 后续选项、小问或题目组成部分
- 当前题号与下一题号之间的连续文字区域

纠错只恢复有充分题目证据的行，不会按固定页码、文件名或测试样本生成结果。

## 6. 第二阶段：OCR 引导的几何框题

第二阶段再次把页面原图发送给视觉模型，同时提供：

1. 当前学科范围。
2. 第一阶段得到的坐标文字行。
3. 从文字行中提取的 `question_anchor` 题号锚点。

模型为每一道独立题目返回一个边界框：

```json
{
  "detections": [
    {
      "id": "draft-1",
      "localOrder": 1,
      "confidence": 0.9,
      "normalizedBBox": {
        "x1": 100,
        "y1": 100,
        "x2": 900,
        "y2": 300
      }
    }
  ]
}
```

### 6.1 当前框题提示词

以下是当前静态提示词。实际请求会动态插入学科范围、坐标文字行和题号锚点 JSON。

```text
你是试卷版面分析助手。
当前文件主要学科范围是：<动态学科范围>。
只关注题目框选、题目顺序和跨页候选。
不要输出题目分类、章节目录、知识点标签。
OCR role 为 knowledge_note、directory、header、footer 或 other 的内容属于排除区；如果一个区域只有这些内容，不得输出题框。
知识点梳理、概念定义、公式总结、方法归纳、章节目录、索引、答案解析和纯讲解都不是题目，即使它们带有序号也不得输出题框。
只有包含 question_anchor/question_content 的独立题目，或包含 question_continuation 的页首续题，才允许输出题框。
每一道独立题目输出一个题框；同一道题只允许输出一个题框，不要重复框选。
禁止输出页面级大框、分栏框或选项独立框；选择项、图表和小问应包含在所属题目的同一个框内。
每个题框必须包含题目的完整首行和完整末行，并在不吞入相邻题目的前提下保留少量安全留白。
相邻题框不得重叠；以上一道题完整末行为下边界、下一道题题号或完整首行为上边界，并在两框之间保留可见间隔。
每个题框不得包含下一道题的题号文字行；宁可在两题之间留下窄间隔，也不要让上下题框共享任何文字。
紧邻某个题号之前、用于说明学年、年级、地区、学校或考试类型的命题来源说明行，必须归入它后面的题目，不得留在上一道题框内。
如果页首在本页第一个新题号之前存在正文、公式、选项或图表，必须将其作为独立的页首续题区域输出一个题框；后续跨页阶段会把它并入前一页末题。
先识别题号和题干起始位置，再按页面阅读顺序设置 localOrder；不要把页眉、页脚、装订线或答案区当作题目。
优先使用下面的坐标文字行与题号锚点确定边界；原图用于补充公式、图表和 OCR 未覆盖的内容。
坐标文字行：<动态 JSON>
题号锚点：<动态 JSON>
坐标使用 0-1000 的归一化坐标系，左上角为原点。
返回严格 JSON：{ "detections": [{ "id": "draft-1", "localOrder": 1, "confidence": 0.9, "normalizedBBox": { "x1": 100, "y1": 100, "x2": 900, "y2": 300 } }] }。
如果无法确定，则保留为空并等待人工复核。
```

## 7. AI 结果的本地后处理

AI 返回结果后不会直接写入题库，而是依次执行以下处理。

### 7.1 数据规范化

- 坐标限制在 `0-1000`。
- 反向坐标会自动交换。
- 宽度或高度小于 `20/1000` 页面的无效框会被丢弃。
- 缺失或非法置信度使用默认值，并限制在 `0-1`。

### 7.2 语义过滤和补漏

- 仅包含知识点、目录或页眉页脚的框会被过滤。
- 如果 OCR 已识别出题号锚点，但 AI 框题结果没有覆盖该锚点，本地逻辑会根据该锚点到下一题号之间的题目文字补建一个题框。
- 自动补框的默认置信度为 `0.75`，仍可在预览中人工调整。

### 7.3 去重和重叠拆分

- 使用交并比和包含率识别近似重复框，保留置信度更高的结果。
- 同栏题框水平重叠比例达到阈值时，才按上下题处理；这可以避免把双栏页面左右两题错误切开。
- 上下相邻框发生重叠时，在两框交界附近重新分割，并保留最小可见间隔。

### 7.4 安全留白和边界对齐

- 默认水平方向扩展 `8/1000` 页面宽度。
- 默认垂直方向扩展 `15/1000` 页面高度。
- 扩展后再次执行重叠拆分，避免安全留白导致题框重新覆盖。
- 学年、年级、地区、学校和考试类型等命题来源说明会对齐到后一道题，而不是被上一题吞入。

### 7.5 像素坐标转换

规范化坐标最终按实际页面宽高转换为像素坐标，并生成 `QuestionDraft`：

```ts
{
  pageIds: [pageId],
  primaryPageId: pageId,
  bboxByPage: {
    [pageId]: { x, y, width, height }
  },
  source: "ai",
  status: "geometry_draft",
  confidence,
  crossPageGroupId: null
}
```

## 8. 框题与跨页合并的职责边界

框题阶段按单页运行，不直接创建跨页实体。

当页面顶部在第一个新题号之前存在题干、公式、选项或图表时，框题阶段会把它输出为本页的独立续题片段。后续跨页检测会比较：

- 前一页最后一道题是否在页尾中断。
- 后一页页首是否没有新题号。
- 两页文字和语义是否连续。

只有跨页候选经过自动流程生成并由用户复核接受后，两个页面片段才会合并成一个具有多个 `pageIds` 和 `bboxByPage` 的题目实体。

因此，在跨页复核前看到两个临时题号不代表框题失败。

## 9. 并发与进度语义

- 客户端以“页”为单位并发调用框题 API。
- 默认框题并发为 `12`，配置允许提高，但代码上限为 `25`。
- 单页通常包含两次顺序 AI 调用：整页 OCR 完成后，才会开始该页框题。
- 如果页面已经有带语义角色的 OCR 行，第一阶段可以跳过。
- 页面进度只在该页整个框题请求成功或失败后递增。

这意味着全量 OCR 或上游路由较慢时，进度条可能暂时保持不动，随后连续跳动。这是当前进度粒度的结果，不等同于本地服务死锁。

## 10. 失败处理

- 第一阶段 OCR 失败时，不会继续用伪造文字生成占位题框。
- 第二阶段框题失败时，API 返回脱敏诊断信息和空结果。
- 客户端把本地 fallback 识别为自动工作流失败，不会将空占位结果当作成功。
- AI 错误写入脱敏日志，只记录任务、模型、尝试次数、耗时和诊断类型等排障字段。
- 日志和接口响应不得包含密钥或完整鉴权信息。

## 11. 已知边界

1. 全量坐标 OCR 显著提高了知识点过滤和题目边界准确率，但增加了首轮处理时间。
2. AI 视觉输出仍具有非确定性，本地几何规则只能修正可验证的重复、重叠和漏锚点问题。
3. 双栏页面依赖正确的横向坐标和栏位关系；本地重叠拆分仅处理具有足够水平重叠的同栏题框。
4. 公式、图片和复杂表格可能没有完整 OCR 文本，因此第二阶段必须继续读取原图。
5. 框题阶段只负责几何草稿，最终跨页关系、题号和分类由后续阶段处理。

## 12. 相关测试

当前行为主要由以下测试覆盖：

- `tests/unit/teachhelper-codex-agent.test.ts`
- `tests/unit/ai-routes.test.ts`
- `tests/unit/analysis-service.test.ts`
- `tests/unit/home-page-workflow.test.tsx`
- `tests/unit/home-page-cross-page-workflow.test.tsx`
- `tests/unit/workflow-concurrency.test.ts`

本文档是对现有实现的说明。修改框题提示词、语义角色、几何阈值、并发策略或跨页职责时，应同步更新相应测试与本文档。
