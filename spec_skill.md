## 技能元数据 (用于配置)- **技能名称**: `Spec Coding`- **适用范围**: 全局技能- **触发描述**:
当进行任何非平凡的编码任务时自动触发，包括但不限于：设计新功能、重
构现有代码、修复 Bug、编写测试或进行技术方案探讨。如果不确定是否使用，
默认使用此技能。--
## 核心原则
在执行任何操作前，必须遵循以下不可妥协的原则：
1. **设计先行**：禁止在未明确需求和设计方案前编写任何实现代码。
2. **测试驱动 (TDD)**：任何新功能或 Bug 修复必须先编写失败的测试用例，
再编写实现代码。
3. **问题驱动**：调试时必须先找到根本原因，严禁盲目尝试修复。
4. **小步提交**：将任务拆解为极小的步骤，每一步都应可独立验证。--
## 标准工作流
请严格按照以下阶段顺序执行：
### 阶段 1：需求澄清与设计
**禁止行为**：直接编写代码。
**执行步骤**：
1. **理解上下文**：阅读相关文档、现有代码结构和最近的提交记录。
2. **需求澄清**：
* 一次只问一个问题。
* 明确目标、约束条件和验收标准。
* 如果任务包含多个独立子系统，建议拆分为子项目分别处理。
3. **方案提议**：
* 提出2-3 种实现方案，分析利弊并给出推荐。
* 按模块展示设计（架构、数据流、接口定义）。
* **关键**：遵循项目现有的代码模式和风格，仅在必要处改进。
4. **设计文档**：生成设计文档（建议存放于
`docs/superpowers/specs/`），并获得用户确认后方可进入下一阶段。
### 阶段 2：制定实施计划
**触发条件**：设计方案已确认。
**执行步骤**：
1. **范围检查**：如果设计过大，拆分为多个独立的计划。
2. **文件规划**：明确列出需要创建或修改的文件及其职责。
3. **任务拆解**：
* 将工作拆解为一系列原子任务。
* 每个任务应极小（预计 2-5 分钟完成），例如："创建测试文件"、"
编写失败测试"、"运行测试确认失败"。
4. **输出计划**：生成包含具体步骤、文件路径、执行命令和预期结果的任
务清单。
### 阶段 3：测试驱动实现 (TDD Implementation)
**适用场景**：所有新功能开发、Bug 修复或重构。
**铁律**：没有失败的测试，禁止编写实现代码。
**Red-Green-Refactor 循环**：
1. **Red (红灯)**：
* 编写一个描述行为的失败测试。
* 运行测试，**确认它是因为正确的原因失败**（而非语法错误）。
2. **Green (绿灯)**：
* 编写**最少**的代码使测试通过。
* 禁止在此步骤添加额外功能或进行重构。
* 运行测试确认通过。
3. **Refactor (重构)**：
* 在测试通过的前提下优化代码结构。
* 消除重复，提高可读性。
* **禁止**在此步骤改变程序行为。
**验证清单**：- [ ] 所有新函数/方法都有对应测试？- [ ] 是否亲眼看到每个测试失败过？- [ ] 是否编写了最小实现？- [ ] 是否覆盖了边界情况？
### 阶段 4：系统性调试
**适用场景**：遇到 Bug、测试失败或异常行为。
**铁律**：根本原因未确定前，禁止修改代码。
**调试流程**：
1. **根因调查**：
* 仔细阅读错误堆栈，稳定复现问题。
* 检查最近的代码变更、依赖更新或配置修改。
* 通过日志或诊断代码定位问题层级。
2. **模式分析**：
* 寻找项目中类似的正常工作代码进行对比。
* 列出正常代码与异常代码的所有差异点。
3. **假设验证**：
* 明确记录假设："我认为 X 是根本原因，因为 Y"。
* 每次只改变一个变量进行验证。
4. **实施修复**：
* 先为Bug 编写一个失败测试。
* 仅针对根本原因进行修复。
* 确认所有测试通过，并未引入新问题。
### 阶段 5：开发收尾
**触发条件**：所有任务完成，测试通过。
**执行步骤**：
1. **最终验证**：运行完整测试套件，确保无回归。
2. **分支管理**：
* 确认基线分支。
* 向用户提供选项：
1. 合并到本地基线分支。
2. 推送并创建 Pull Request。
3. 保留分支自行处理。
4. 放弃此次工作。--
## 危险信号
如果出现以下想法，说明正在偏离正确流程，**必须立即停止**：- "这个问题很简单，不需要写测试/设计。"- "我先写实现，回头再补测试。"- "我先改改代码试试能不能修好。" (调试时)- "这个 Bug 太明显了，肯定就是这里的问题，不用找证据了。"