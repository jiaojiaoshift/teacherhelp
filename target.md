```markdown
# 教培智能题库工作台 - 完整 Vibe Coding Prompt

> **使用说明**：
> 1. 请严格按照三个阶段顺序执行，该阶段已完成才可进入下一阶段，严禁跳步
> 2. 在需求确认和阶段设计确认完成后，同一会话内默认连续自动执行当前阶段的剩余工作；除非遇到真实阻塞、阶段切换确认点或最终验收，否则不要停下来等待用户回复“继续”
> 2. 所有代码需完整可运行，不要遗漏任何导入或配置
> 3. 技术栈：Next.js 14 + React 18 + Tailwind CSS + TypeScript
> 4. 项目已配置好 `.cursorrules` Skill 文件，请遵循其中的代码规范和设计风格
> 5. 阶段内默认采用小步快跑连续执行；一个切片、自测或单个功能模块完成后，如果当前阶段仍未完成，应立即继续下一切片，而不是请求用户再次输入
> 6. 非必要不输出阶段内总结；仅在以下情况汇报：a) 阶段完成，b) 出现真实阻塞，c) 用户明确要求查看进度，d) 最终验收
> 7. 如需说明设计思路，仅在阶段切换、真实阻塞或用户明确要求时，用 1-2 句简要说明
> 8. 项目以完整网站形式构建，包含导航系统、页面路由和响应式布局

---

## 会话执行规则（高优先级）

- 在同一个会话中，只要当前阶段目标尚未完成，就默认任务仍在继续，不要因为“已经完成一个模块”“已经通过一次测试”“已经给出一次总结”而自行结束任务
- 不要把“请告知我”“验证通过后告知我”“等待下一步指令”理解为阶段内每个模块都要暂停；除非当前阶段整体完成、需要进入下一阶段，或出现无法自行解决的外部阻塞
- 若当前阶段还有未完成事项，完成一个切片后应自动进入下一个切片，持续执行
- 若外部运行环境客观中断，应在恢复后从最近状态继续，而不是把中断误判为任务自然完成

---

## 项目初始化：网站框架搭建

在开始阶段一之前，请先搭建整个网站的框架：

**【项目创建】**
- 使用 `create-next-app` 创建 Next.js 14 项目，启用 TypeScript 和 Tailwind CSS
- 安装核心依赖：
  ```bash
  npm install pdfjs-dist pdf-parse tesseract.js openai idb zustand katex react-markdown
  npm install -D @types/pdf-parse
```
- 配置 `tailwind.config.ts`，扩展主题色为教育行业友好的蓝绿配色系

**【网站整体布局】**

- 顶部导航栏（Navbar）：Logo + 应用名称"智题库" + 全局搜索框 + 快捷操作按钮
- 左侧边栏（Sidebar）：可折叠，包含：
  - 文件管理（上传的文件列表）
  - 文件夹树（题库分类）
  - 标签管理入口
  - AI 组卷入口
  - 笔记本入口
  - 组卷历史入口
- 主内容区（Main）：动态渲染对应页面内容
- 右侧面板（Drawer）：点击题目详情时滑出，宽度约 400px

**【路由设计】**
- `/` - 首页（题库总览，显示所有题目）
- `/folder/[id]` - 文件夹视图
- `/exam/create` - 组卷工作台
- `/exam/history` - 组卷历史
- `/notebooks` - 笔记本列表
- `/notebooks/[id]` - 单个笔记本详情
- `/tags` - 标签管理
- `/settings` - 设置页

**【全局状态管理】**
- 使用 Zustand 创建全局 store：
  - `fileStore`：上传文件列表、当前选中页面
  - `questionStore`：所有题目数据、当前题目、多选列表
  - `folderStore`：文件夹树结构、当前文件夹
  - `tagStore`：所有标签、标签使用统计
  - `examStore`：当前组卷题目列表
  - `noteStore`：笔记数据

**【数据持久化】**
- 所有数据存储在 IndexedDB 中，使用 `idb` 库封装
- 创建 `lib/db.ts` 统一管理数据库操作
- 每次数据变更自动保存，刷新页面不丢失

---

## 阶段一：PDF/图片上传 + 题目分块 + 智能标签 + 文件夹管理

> **本阶段目标**：用户上传 PDF 或图片后，系统自动框选每道题、识别内容、打标签，并支持文件夹分类管理。
>
> ⚠️ **请完整实现本阶段所有功能，确认可运行后，进入阶段二。**

---

### 1.1 上传与渲染

请帮我实现以下功能：

**【上传区域】**
- 在首页主区域顶部设计一个大的拖拽上传区
- 虚线边框样式，中间有上传图标和"拖拽文件到此处或点击上传"文字
- 支持的文件类型显示在下方（PDF、PNG、JPG）
- 支持点击弹出文件选择框，也支持拖拽文件到该区域
- 上传时显示进度条动画
- 上传完成后，文件出现在左侧边栏的"文件列表"中，Toast 提示上传成功
- 如果文件格式不支持，Toast 提示错误

**【PDF 渲染为图片】**
- 使用 `pdfjs-dist` 库处理 PDF 文件
- 配置 `pdfjs.GlobalWorkerOptions.workerSrc` 指向 CDN worker
- 将 PDF 每一页渲染为高清 Canvas 图片
- 渲染时 scale 设为 2.0，确保图片足够清晰
- 每页图片转为 base64 格式存储
- PDF 的每一页作为独立项出现在左侧文件列表中，命名格式：`{文件名}_第{X}页`
- 渲染过程显示"正在处理 PDF，共 X 页"的进度提示

**【页面查看与切换】**
- 左侧"文件列表"展示所有已上传的文件和 PDF 页面
- 每个列表项显示缩略图（图片直接缩略，PDF 页显示第一页缩略图）+ 文件名
- 当前选中的页面有蓝色高亮边框和浅蓝背景
- 点击某一页后，中间主区域显示该页大图
- 大图可缩放查看（鼠标滚轮缩放，拖拽平移）
- 支持键盘 ↑ ↓ 键快速切换上下页
- 支持键盘 Delete 键删除选中页面

---

### 1.2 版面分析与题目分块（核心功能）

**【调用 Vision API 进行版面分析】**
- 选中某一页后，页面顶部工具栏出现"🔍 分析本页题目"按钮（蓝色醒目按钮）
- 点击后按钮变为加载状态，显示"AI 正在识别题目..."
- 将当前页面图片（base64）发送给 OpenAI Vision API
- 使用的模型：`gpt-4o-mini`（支持视觉识别）
- 请求参数设置：`max_tokens: 4096`，`temperature: 0.1`（确保输出稳定）

**Vision API 的 System Prompt 请严格按如下设置：**
```
你是一个教育版面分析专家，擅长分析试卷和习题集的页面布局。
请分析这张试卷/习题图片，完成以下任务：

1. 找出页面上每一道独立题目。注意：
   - 题目可能跨多行，需要完整框选
   - 选择题的选项（A/B/C/D）属于题目的一部分
   - 如果题目有小题（如"（1）""（2）"），每个小题视为独立题目
   - 页眉页脚、页码、密封线等内容不要框选
2. 用矩形框精确框选每道题的完整区域，返回每个区域的像素坐标（以图片左上角为原点）
3. 识别每个框选区域内的完整文字内容（包括题干、选项、填空横线、图示说明等）
4. 判断每道题的题型：
   - 选择题（有明确 ABCD 选项）
   - 填空题（有下划线或括号留空）
   - 简答题（需要文字作答）
   - 证明题（要求证明某个结论）
   - 计算题（需要计算过程）
   - 其他
5. 分析每道题所属的学科章节，尽量细化到小节级别
   （如"二次函数顶点式"、"立体几何-线面垂直"、"力学-牛顿第二定律"）
6. 分析每道题考察的核心考点，给出 2-5 个关键词
   （如"判别式应用"、"配方法"、"顶点坐标公式"）

请严格返回以下 JSON 格式，不要包含任何额外解释或 Markdown 代码块标记：
{
  "questions": [
    {
      "id": 1,
      "bbox": { "x": 50, "y": 120, "width": 700, "height": 200 },
      "type": "选择题",
      "text": "已知二次函数 f(x)=x²+2x+1，则 f(x)的最小值为（  ）\nA. -1  B. 0  C. 1  D. 2",
      "chapterTag": "二次函数-最值问题",
      "knowledgeTags": ["函数最值", "配方法", "顶点坐标"]
    }
  ]
}

注意：
- bbox 的 x、y、width、height 均为像素整数值
- 如果一页有多道题，每道题独立一个对象
- 如果无法确定章节，chapterTag 填写"未分类"
- 文字识别尽量完整准确，数学公式用 LaTeX 格式表示（如 $x^2+2x+1$）
- 框选区域不要重叠，每道题独立成区
```

**【框选结果可视化渲染】**
- 收到 API 返回的 JSON 数据后，在原图上方叠加一个 SVG 层
- SVG 层与图片完全等大等位置
- 每道题绘制一个矩形框，样式按题型区分：
  - 选择题：蓝色边框（#3B82F6），浅蓝半透明填充（rgba(59,130,246,0.12)）
  - 填空题：绿色边框（#10B981），浅绿半透明填充
  - 简答题：橙色边框（#F59E0B），浅橙半透明填充
  - 证明题：紫色边框（#8B5CF6），浅紫半透明填充
  - 计算题：红色边框（#EF4444），浅红半透明填充
  - 其他：灰色边框
- 每个框的左上角有一个圆形标签，显示题号（如"Q1""Q2"），白色文字，颜色与边框一致
- 框的右下角显示题型小图标（选择=☐、填空=✎、简答=📝、证明=🔷）

**【框选交互效果】**
- 鼠标悬停在框上时：
  - 框的透明度降低（更明显）
  - 出现白色提示浮层（tooltip），显示题目文本前 80 个字
  - 光标变为 pointer
- 点击某个框选区域：
  - 该框变为选中状态（边框加粗、颜色更深、有外发光效果）
  - 右侧详情面板滑出（Drawer），显示该题的完整信息
- 点击图片空白区域或按 Esc 键，取消选中，右侧面板收起
- 支持键盘 Tab 键在不同框之间切换选中

**【右侧详情面板内容】**
- 面板标题："题目详情 #X"
- 题型标签（彩色大标签）+ 题号
- 完整识别文字区域（白色可编辑文本框，用户可修正 OCR 错误）
- 章节标签（chapterTag）：深色圆角标签，可点击编辑
- 考点标签列表（knowledgeTags）：浅色标签，每个标签可单独删除
- "添加自定义标签"按钮和输入框
- "移动到文件夹"下拉选择器
- 底部操作按钮："保存修改"、"添加笔记"、"删除此题"
- 如果文字有修改，显示"未保存"提示，点击保存后同步更新

**【错误处理】**
- 如果 API 调用失败，显示错误提示："识别失败，请重试"，并提供重试按钮
- 如果返回 JSON 解析失败，显示原始返回内容，提示"格式异常"
- 如果某页识别结果为空（0 道题），提示"本页未检测到题目"
- 设置 API 超时时间为 60 秒

---

### 1.3 标签系统

**【自动标签展示与管理】**
- 每道题的章节标签（chapterTag）以深蓝底白字圆角标签显示
- 考点标签（knowledgeTags）以柔和彩色圆角标签显示
- 预设 8 种标签颜色循环使用：蓝、绿、橙、紫、粉、青、黄、灰
- 相邻标签自动使用不同颜色
- 自定义标签以虚线边框样式显示，与自动标签区分

**【自定义标签功能】**
- 每道题详情面板和题目卡片上，标签行末尾有"+ 添加标签"小按钮
- 点击弹出小型输入框（Popover），输入标签名后回车确认
- 自定义标签同样可选择颜色（下拉 8 色选择器）
- 所有标签右侧悬停时显示"×"删除按钮
- 标签文字支持双击编辑修改

**【标签管理页面】（路由：/tags）**
- 展示所有已使用的标签列表，以卡片或表格形式
- 按使用次数降序排列，每个标签显示：
  - 标签名称 + 颜色标识
  - 关联题目数量
  - 最近使用时间
- 支持操作：
  - 重命名标签：内联编辑，修改后所有关联题目同步更新
  - 合并标签：选择"合并到..."，弹出标签选择器，合并后原标签删除
  - 删除标签：仅可删除关联题目数为 0 的标签
- 顶部搜索框可快速查找标签
- 批量选择标签进行合并或删除

**【标签过滤功能】**
- 在题库页面和文件夹视图中，点击任意标签自动触发筛选
- 筛选后只显示包含该标签的题目
- 顶部显示"当前筛选：XXX 标签"，旁边有"×"清除筛选
- 支持多标签组合筛选（AND 逻辑）

---

### 1.4 文件夹管理系统

**【文件夹树组件】**
- 位于左侧边栏"题库分类"区域
- 树形结构，类似 VS Code 文件浏览器
- 根节点为"📁 我的题库"，默认展开
- 每个节点显示：
  - 展开/折叠箭头（有子文件夹时）
  - 文件夹图标（📁 默认 / 📂 展开状态）
  - 文件夹名称
  - 题目数量角标（灰色小数字，如"12"）
- 当前选中的文件夹有蓝色高亮背景

**【文件夹右键菜单】**
- 在文件夹节点上右键，弹出上下文菜单：
  - 📂 新建子文件夹
  - ✏️ 重命名
  - 📋 复制文件夹路径
  - 🗑️ 删除文件夹
  - 📊 查看统计信息
- 菜单有弹出动画，点击菜单外区域关闭

**【文件夹操作实现】**
- **新建文件夹**：
  - 树顶部"+ 新建文件夹"按钮
  - 右键菜单"新建子文件夹"
  - 点击后在树中插入输入框，输入名称回车确认
  - 名称为空时使用默认名"未命名文件夹"
  - 不能创建同名同级文件夹，冲突时提示

- **重命名**：
  - 右键→重命名，文件夹名变为可编辑输入框
  - 回车确认，Esc 取消
  - 修改后文件夹路径面包屑同步更新

- **删除文件夹**：
  - 右键→删除，弹出确认弹窗
  - 弹窗内容："确定要删除文件夹「{文件夹名}」吗？其中的 {X} 道题目将移入「未分类」文件夹。"
  - 确认后执行删除和题目迁移
  - Toast 提示"文件夹已删除，X 道题已移至未分类"

- **移动文件夹**：
  - 支持拖拽文件夹到目标文件夹上
  - 拖拽时显示蓝色插入线提示放置位置
  - 不能将文件夹拖入自己的子文件夹（防止循环）
  - 移动后所有子文件夹和题目路径自动更新

**【文件夹与题目的关联操作】**
- 每道题目卡片底部显示当前所属文件夹路径面包屑
- 面包屑可点击快速跳转到对应文件夹
- 题目卡片上有"📂 移动到..."下拉按钮
  - 弹出文件夹选择器（树形结构，可搜索）
  - 选择目标文件夹后，题目立即移动
  - Toast 提示"题目已移至 {文件夹名}"
- **批量移动**：
  - 题目卡片支持多选模式
  - 勾选多道题后，顶部出现批量操作栏
  - 批量操作栏包含："已选 X 题"、"移动到文件夹"、"批量添加标签"、"批量删除"
  - 批量删除需要二次确认

**【文件夹视图页面】（路由：/folder/[id]）**
- 顶部面包屑导航：我的题库 > 数学 > 二次函数
- 文件夹信息栏：文件夹名、题目总数、创建时间
- 搜索和筛选工具栏：
  - 搜索框（搜索题目文本和标签）
  - 题型筛选下拉框（多选）
  - 排序方式（按时间/按题号/按题型）
- 题目卡片网格或列表展示（可切换视图模式）
- 空文件夹显示空状态插画："此文件夹为空，上传或拖入题目吧"

---

### 1.5 题目卡片设计

**【卡片样式规范】**
- 白色背景，12px 圆角，轻微阴影（hover 时阴影加深）
- 卡片左侧有 4px 宽的彩色竖条（颜色对应题型）
- 卡片头部区域：
  - 左侧：题号 + 题型标签（小号彩色圆角标签）
  - 右侧：操作按钮组（编辑、移动、删除，hover 时显示）
- 卡片主体：
  - 题目文字，最多显示 4 行，超出折叠
  - 折叠时末尾显示"...展开全部"，点击展开
  - 数学公式使用 KaTeX 渲染（行内 $...$ 和块级 $$...$$）
- 卡片底部：
  - 考点标签列表（最多显示 4 个，超出显示"+N"）
  - 文件夹路径面包屑（小号灰色文字）
  - 底部操作栏：笔记按钮、多选复选框

**【题目卡片交互】**
- hover 时整体上浮 2px，阴影加深
- 点击卡片主体 → 展开题目完整内容 / 或打开右侧详情面板
- 点击标签 → 触发标签筛选
- 点击文件夹路径 → 跳转到对应文件夹
- 多选模式：卡片左侧出现圆形复选框，点击进入多选

**【题目搜索与筛选】（首页和文件夹视图通用）**
- 全局顶部搜索栏（在 Navbar 中）
- 输入关键词后实时过滤（300ms 防抖）
- 搜索范围：题目文本、标签名、章节名、文件夹名
- 搜索结果中匹配文字用黄色高亮标记
- 无结果时显示："未找到相关题目，试试其他关键词" + 清除筛选按钮
- 搜索结果按相关度排序

---

> ✅ **阶段一完成标准检查清单**：
> - [ ] 可上传 PDF 和图片文件
> - [ ] PDF 正确渲染为高清图片
> - [ ] 点击"分析本页"后 Vision API 成功返回 JSON
> - [ ] 图片上正确显示彩色框选区域
> - [ ] 点击框选区域右侧面板显示题目详情
> - [ ] 标签可添加、编辑、删除
> - [ ] 文件夹树可新建、重命名、删除、拖拽移动
> - [ ] 题目可移动到不同文件夹
> - [ ] 题目卡片样式正确，搜索筛选可用
> - [ ] 数据刷新后不丢失（IndexedDB 持久化）
>
> 📌 **请实现完以上所有功能并自测通过后告知我。验证通过后我们进入阶段二。**

---

## 阶段二：AI 智能解析 + 专题组卷 + 讲义生成

> **⚠️ 前置条件：阶段一已完整实现并验证通过。**
>
> **本阶段目标**：接入大模型对所有题目进行深度解析，自动聚合相似考点题目生成专题卷，支持交互式组卷并导出讲义。
>
> ⚠️ **请完整实现本阶段所有功能，确认可运行后，再进入阶段三。**

---

### 2.1 AI 题目深度解析

**【批量解析功能入口】**
- 在题库页面和文件夹视图顶部操作栏添加"🤖 AI 批量解析"按钮
- 按钮样式：渐变蓝紫色背景，白色文字，带星标图标，醒目但不突兀
- 点击后弹出配置弹窗（Modal）：

**配置弹窗设计：**
- 标题："AI 题目深度解析"
- 解析范围选择（Radio）：
  - ○ 当前文件夹（共 X 题）
  - ○ 全选题目（共 X 题）
  - ○ 自定义选择（已选 Y 题）
- 解析内容选项（Checkbox 多选，默认全选）：
  - ☑ 详细解题步骤
  - ☑ 最终答案
  - ☑ 考点分析与易错点
  - ☑ 难度评估
  - ☑ 扩展标签（用于组卷）
- 模型选择下拉框：gpt-4o-mini（推荐）/ gpt-4o（更精准）
- 预估消耗：显示预计消耗 token 数
- 底部按钮："开始解析"、"取消"

**【解析执行过程】**
- 点击"开始解析"后，弹窗变为进度面板
- 显示进度条 + 百分比 + "正在解析第 X/Y 题..."
- 每道题解析完成后，进度条实时更新
- 支持"暂停"和"取消"操作
- 如果某题解析失败，标记为失败并继续下一题，最后汇总失败列表

**解析 API 的 System Prompt：**
```
你是一位资深教研员和学科专家，拥有 20 年教学经验。
请对以下题目进行深度解析，帮助学生和教师理解这道题。

请返回严格的 JSON 格式（不要包含 Markdown 代码块标记）：
{
  "questionId": "原题的唯一ID",
  "solution": "详细解题步骤，使用 Step 1、Step 2... 格式分步说明。数学公式使用 LaTeX 格式 $...$ 或 $$...$$。每一步说明解题思路和计算过程。",
  "answer": "最终答案。选择题填选项字母（如 B），填空题填完整答案，简答题给出最终结论",
  "analysis": "考点深度分析：\n1. 本题考察的核心知识点\n2. 解题的关键思路和突破口\n3. 学生常见的错误和易错点提醒\n4. 解题技巧总结",
  "difficulty": "easy / medium / hard",
  "estimatedTime": 5,
  "extendedTags": ["三级标签1", "三级标签2", "四级标签1"],
  "similarQuestionSuggestion": "如果出一道变式题，可以如何改编（一句话简短建议）"
}

要求：
- 解题步骤清晰易懂，适合学生自学阅读
- 考点分析要具体深入，不要泛泛而谈
- 难度评估参考标准：easy=基础概念题，medium=综合应用题，hard=压轴题/竞赛题
- estimatedTime 为预估解题时间，单位分钟，填数字
- extendedTags 要比原始标签更细化，便于后续组卷精确聚合
  （例如原标签"二次函数"，扩展为"二次函数顶点式"、"二次函数最值应用"、"二次函数图像变换"）
- similarQuestionSuggestion 简短一句话即可，说明改编方向
```

**【解析结果展示设计】**
- 每道题卡片下方新增可折叠的"💡 AI 解析"区域
- 折叠状态（默认）：
  - 蓝色竖线标识在左侧
  - 显示一行摘要：难度标签 + 预估时间 + "点击展开查看完整解析"
  - 答案模糊显示（文字虚化），防止学生直接看答案
- 展开状态（点击展开）：
  - **解题步骤区**：深色背景代码块风格，分 Step 展示，等宽字体
  - **最终答案区**：浅绿色背景卡片，答案文字加粗，前面有"✅ 答案："标签
  - **考点分析区**：浅蓝色背景卡片，引用块样式，分段清晰
  - **易错点提醒区**：浅黄色背景卡片，前面有"⚠️ 注意："标签
  - **扩展标签区**：虚线边框小标签，可点击加入筛选
  - **变式题建议**：灰色斜体文字
- 解析区域底部有评价按钮：👍 有帮助 / 👎 有误（点击记录反馈）
- 如果该题没有解析数据，显示"暂无解析，点击 AI 解析生成"

**【解析数据存储】**
- 解析结果存入题目的 `analysisData` 字段
- 同步存入 IndexedDB
- 如果题目已有解析，再次点击"AI 解析"会提示"已有解析，是否覆盖？"

---

### 2.2 智能专题组卷

**【组卷工作台页面】（路由：/exam/create）**
- 独立页面，三栏布局
- **左栏（题目候选池）**：宽度约 300px，可收起
- **中栏（试卷编辑区）**：flex-1，A4 纸预览
- **右栏（AI 建议面板）**：宽度约 350px，可收起

**【左栏 - 题目候选池】**
- 顶部搜索框（搜索全部题库）
- 筛选条件：按文件夹、按标签、按题型、按难度
- 题目列表以紧凑卡片展示，每项显示：
  - 题号 + 题型图标
  - 题干前 80 字
  - 难度标签和知识点标签
- 每道题右侧有"+ 添加"按钮
- 支持拖拽题目到中栏编辑区
- 已添加的题目显示"✓ 已添加"灰色标记，不可重复添加
- 底部显示"共 X 题，已选 Y 题"

**【AI 智能推荐专题】**
- 左栏顶部有"✨ AI 智能推荐"标签切换
- 切换到推荐模式，AI 自动分析题库标签：
  - 统计所有题目的 chapterTag + knowledgeTags + extendedTags
  - 将三级/四级标签相同或高度相似的题目自动聚合
  - 生成 5-10 个"专题候选集"
- 每个候选集显示为卡片：
  - 专题名称（AI 自动生成，如"二次函数最值问题综合练习"）
  - 题目数量 + 难度分布小图（简易柱状图或饼图）
  - 覆盖考点标签列表
  - "预览题目"按钮 → 展开题目列表
  - "一键加入组卷"按钮 → 全部添加到中栏

**【中栏 - 试卷编辑区】**
- A4 纸张比例预览（210mm × 297mm 等比缩放）
- 白色背景，带阴影边框
- 编辑区结构（从上到下）：
  - **试卷标题区**（可编辑）：输入试卷标题，默认"未命名试卷"
  - **试卷信息行**（可编辑）：考试时间、满分、适用班级等
  - **分节标记**：可插入"一、选择题"、"二、填空题"等分节标题
  - **题目列表**：每道题显示题号、题目内容、题型标识
  - 题目支持拖拽排序（上下拖拽调整位置）
  - 每道题右侧有"× 移除"按钮
  - 题目去重检测：如果重复添加同一题，提示"该题已在试卷中"
- 顶部工具栏：
  - "添加分节标题"按钮
  - "清空试卷"按钮
  - 题目数量统计
  - "AI 分析试卷"按钮

**【右栏 - AI 建议面板】**
- AI 实时分析当前试卷结构，给出建议：
  - "📊 当前试卷分析：已选 X 题，覆盖 Y 个考点"
  - "📈 难度分布：简单 X 题、中等 Y 题、困难 Z 题"
  - "⚠️ 建议：难度分布偏简单，建议增加中等难度题目"
  - "🔍 未覆盖考点：XXX、XXX，是否从题库中推荐？"
- AI 对话区域：
  - 用户可用自然语言与 AI 交互调整试卷
  - 输入框示例 placeholder："帮我找 3 道关于韦达定理的填空题"
  - AI 回复以气泡形式显示，包含可操作按钮
  - 如 AI 推荐题目，显示题目卡片 + "添加"按钮
- 对话历史保留在当前会话中

**【AI 对话交互示例】**
```
用户："去掉所有难度为 hard 的题"
AI："已为你移除 3 道 hard 难度题目：第 2 题（二次函数综合）、第 7 题（几何证明）、第 12 题（数列压轴）。当前试卷共 15 题。"
用户："加 5 道关于三角函数的题"
AI："在题库中找到 8 道三角函数相关题目，按难度排序推荐前 5 道：\n[题目卡片列表]\n点击可添加或替换现有题目。"
```

---

### 2.3 讲义生成与导出

**【讲义预览页面】**
- 在组卷编辑区点击"📄 生成讲义"进入预览模式
- 预览为完整 A4 纸张比例，白色背景
- 页面顶部工具栏（预览模式下不打印）：
  - 缩放控制：50% / 75% / 100% / 125% / 150%
  - "编辑模式"切换按钮（返回编辑）
  - "打印"按钮
  - "导出 PDF"按钮
  - "导出 Word"按钮（可选）
  - "保存试卷"按钮

**【讲义内容结构】**
- **封面区域**（第一页）：
  - 讲义标题（大号字体，可编辑）
  - 副标题（可编辑，默认填日期）
  - 适用年级/班级（可编辑）
  - 考试时长 / 满分（可编辑）
  - 装饰分隔线
- **题目区**（从第二页或封面下方开始）：
  - 按分节标题排列
  - 自动题号编号（一、1. (1) 等层级）
  - 每道题格式：
    - 题号 + 题型标识（小标签）
    - 题目正文（数学公式 KaTeX 渲染）
    - 答题留空区域：
      - 选择题：A.____ B.____ C.____ D.____ 格式
      - 填空题：横线留空，长度根据答案自动调整
      - 简答题：预留答题空白区（可拖拽调整高度）
  - 题目之间有明显分隔，大题目不跨页
- **解析区**（末尾或紧随每题之后，可配置）：
  - 标题"参考答案与解析"
  - 每道题的答案（绿色高亮）+ 解题步骤
  - 分页符合理插入

**【讲义编辑功能】**
- 点击任意文字可直接编辑（内联编辑）
- 拖拽题目调整顺序
- 删除单道题（× 按钮）
- 调整答题空位大小（拖拽底部手柄）
- 修改实时反映在预览中
- "恢复默认"按钮重置修改

**【导出功能实现】**
- **打印功能**：
  - 调用 `window.print()`
  - 使用 CSS `@media print` 隐藏所有 UI 元素（导航栏、侧边栏、工具栏）
  - 设置 A4 纸张大小，适当边距（上下 15mm，左右 20mm）
  - 打印前自动分页优化

- **导出 PDF 功能**：
  - 使用 `html2canvas` + `jspdf` 库
  - 或使用 `react-to-print` 库
  - 生成 PDF blob 后触发浏览器下载
  - 文件名格式：`{试卷标题}_{日期}.pdf`
  - 导出过程显示"正在生成 PDF..."加载动画

- **导出 Word 功能**（可选）：
  - 使用 `html-docx-js` 库
  - 生成 .docx 文件下载
  - 保留基本格式

**【试卷保存】**
- 点击"保存试卷"，将当前试卷数据存入 IndexedDB
- 保存内容：试卷标题、题目列表、排序、分节信息、创建时间
- 保存后 Toast 提示"试卷已保存"

---

### 2.4 组卷历史页面（路由：/exam/history）

**【历史列表】**
- 以时间线或卡片列表展示所有保存的试卷
- 每项显示：
  - 试卷标题
  - 创建时间（相对时间，如"3 天前"）
  - 题目数量
  - 缩略预览图（第一页快照）
- 点击进入试卷预览，可再次编辑和导出
- 支持删除历史记录（确认弹窗）
- 支持复制试卷（创建副本）

---

> ✅ **阶段二完成标准检查清单**：
> - [ ] AI 批量解析功能可用，解析结果正确展示
> - [ ] 解析数据持久化存储
> - [ ] 组卷工作台三栏布局正常
> - [ ] AI 可自动聚合专题推荐
> - [ ] 题目可添加到试卷编辑区并拖拽排序
> - [ ] AI 建议面板可交互对话
> - [ ] 讲义预览正确，A4 比例
> - [ ] 打印和导出 PDF 功能可用
> - [ ] 试卷可保存和历史查看
>
> 📌 **请实现完以上所有功能并自测通过后告知我。验证通过后我们进入阶段三。**

---

## 阶段三：笔记系统

> **⚠️ 前置条件：阶段一和阶段二已完整实现并验证通过。**
>
> **本阶段目标**：为每道题添加笔记功能，并建立独立的笔记本体系，与题库深度关联，形成完整的学习工作流。
>
> ⚠️ **请完整实现本阶段所有功能，完成后告知我进行最终验收。**

---

### 3.1 题目关联笔记

**【笔记入口设计】**
- 每道题目卡片底部操作栏有"📝 笔记"按钮
- 如果该题已有笔记内容，按钮显示为"📝 笔记 (N)"，N 为笔记条数
- 在右侧详情面板中同样有笔记入口
- 点击后，在题目卡片下方展开笔记编辑区域（手风琴展开动画）

**【笔记编辑器】**
- 展开区域标题栏："题目笔记"，右侧显示"最后编辑：X 分钟前"
- 工具栏（Markdown 风格，简洁）：
  - **B** 加粗 | *I* 斜体 | ~~S~~ 删除线
  - 分隔符 |
  - 无序列表 | 有序列表 | 引用块
  - 代码块（`</>` 图标）| LaTeX 公式（√x 图标）
  - 插入图片按钮
- 编辑区域为 textarea 或 contentEditable div
- 支持 Markdown 语法输入，实时预览渲染
- 支持 LaTeX 数学公式，用 $...$ 或 $$...$$ 包裹，KaTeX 实时渲染
- 支持 @ 提及其他题目：
  - 输入 @ 后弹出题目搜索浮层
  - 搜索并选中题目后，插入题目引用链接
  - 显示为题目摘要卡片，点击可跳转
- 编辑器高度自适应内容，最小 120px

**【自动保存机制】**
- 输入后 500ms 防抖自动保存到 IndexedDB
- 保存时右上角显示"💾 已保存"时间戳（2 秒后淡出）
- 如果保存失败，显示"⚠️ 保存失败，请重试"
- 切换题目时自动保存当前笔记
- 支持 Ctrl+S / Cmd+S 手动保存

**【多条笔记支持】**
- 笔记区域顶部有标签页切换：
  - "笔记 1" | "笔记 2" | "笔记 3" | "+ 新建"
- 每条笔记独立编辑、独立时间戳
- 支持删除单条笔记（确认弹窗）
- 最多支持 10 条笔记

**【笔记与题目联动】**
- 笔记数据与题目 ID 绑定存储
- 题目被移动到其他文件夹，笔记跟随
- 题目被删除时，笔记同步删除（确认时提醒"将同时删除 X 条笔记"）
- 在题目搜索结果中，可看到题目是否有笔记（小图标标识）

---

### 3.2 独立笔记本系统

**【笔记本页面】（路由：/notebooks）**
- 笔记本列表页面，卡片布局
- 每张笔记本卡片显示：
  - 笔记本封面颜色（6 种柔和颜色可选）
  - 笔记本名称
  - 笔记条目数量
  - 最近编辑时间
  - 预览文字（前 3 行）
- 顶部"+ 新建笔记本"按钮
- 点击笔记本卡片进入笔记本详情页

**【新建笔记本弹窗】**
- 输入笔记本名称
- 选择封面颜色（6 色色块选择器）
- 可选添加描述
- 确认创建

**【笔记本详情页】（路由：/notebooks/[id]）**
- 顶部：笔记本名称（可编辑）+ 颜色标识 + 编辑时间
- 右上角操作按钮：编辑名称、导出、删除笔记本
- 笔记条目列表（类似 Notion 的简洁列表）：
  - 每行显示笔记标题（取第一行文字） + 预览 + 编辑时间
  - 点击展开笔记内容
  - 支持拖拽排序
- 底部"+ 新建笔记条目"按钮

**【笔记本中的题目引用】**
- 在笔记条目编辑器中，支持"引用题目"功能
- 点击工具栏"📎 引用题目"按钮，弹出题目搜索弹窗
- 搜索并选中题目后，在笔记中插入题目引用卡片
- 题目引用卡片显示：
  - 题号 + 题型标签
  - 题干前 100 字
  - "查看原题"链接 → 点击跳转到题目所在文件夹视图
- 引用卡片可删除，不影响原题

**【笔记本导出】**
- 单个笔记本支持导出为：
  - Markdown 文件（.md）
  - PDF 文件
- 导出时保留题目引用信息
- 导出文件名：`{笔记本名称}_{日期}`

---

### 3.3 全局搜索增强

**【搜索范围扩展】**
- 顶部全局搜索栏增强，搜索范围包括：
  - 题目文本
  - 题目标签
  - 笔记内容
  - 笔记本名称
  - 文件夹名称
- 输入关键词后，搜索结果按类型分组显示：
  - 📝 题目（X 个结果）
  - 📄 笔记（Y 个结果）
  - 📁 文件夹（Z 个结果）
  - 📓 笔记本（W 个结果）
- 每组显示前 5 条，更多点击"查看全部"
- 搜索结果中匹配关键词高亮（黄色标记）

**【搜索结果交互】**
- 点击题目结果 → 跳转到题目所在文件夹并高亮该题
- 点击笔记结果 → 跳转到对应笔记本并展开该笔记
- 点击文件夹结果 → 跳转到文件夹视图
- 搜索结果面板按 Esc 关闭

---

### 3.4 笔记数据管理

**【笔记存储结构】**
```
IndexedDB:
- notes 表：
  - id: string (uuid)
  - questionId: string (关联题目，独立笔记本的笔记此字段为 null)
  - notebookId: string (关联笔记本，题目笔记此字段为 null)
  - content: string (Markdown 格式)
  - createdAt: Date
  - updatedAt: Date

- notebooks 表：
  - id: string (uuid)
  - name: string
  - color: string
  - description: string
  - createdAt: Date
  - updatedAt: Date
```

**【数据完整性保护】**
- 删除题目时，检查是否有关联笔记，弹窗提醒
- 删除笔记本时，检查是否有笔记条目，弹窗提醒
- 支持导出所有笔记数据（JSON 格式备份）
- 支持导入笔记数据（合并或覆盖）

---

> ✅ **阶段三完成标准检查清单**：
> - [ ] 题目卡片可展开笔记编辑区域
> - [ ] 笔记编辑器支持 Markdown 和 LaTeX
> - [ ] 笔记自动保存功能正常
> - [ ] 多条笔记切换正常
> - [ ] 笔记本列表页面正常
> - [ ] 笔记本详情页可编辑笔记条目
> - [ ] 题目引用功能正常
> - [ ] 全局搜索增强可用
> - [ ] 笔记数据独立存储，与题目联动正确
>
> 📌 **请实现完以上所有功能并自测通过后告知我，进行最终验收。**

---

## 最终验收标准

完成全部三个阶段后，请确认以下完整工作流可正常运行：

1. **上传 → 识别 → 分类**：上传 PDF → 渲染为图片 → AI 框选识别 → 题目存入题库 → 标签分类 → 文件夹管理
2. **解析 → 组卷 → 导出**：AI 批量解析 → 生成深度解析 → AI 推荐专题 → 手动/自动组卷 → 生成讲义 → 导出 PDF
3. **笔记 → 关联 → 搜索**：添加题目笔记 → 创建独立笔记本 → 引用题目 → 全局搜索 → 笔记导出

所有数据在刷新后保持，整体交互流畅，UI 美观专业。



